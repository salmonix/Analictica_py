# from numpy import array ??

class Table(object):
    def __init__(self, tokens):
        self.tokens = tokens

    def build_from(self,attribute):
        for t in self.tokens:
            

    def format(self):
        # if there is not table -> formatting must fail instead of creating an empty table
        pass
